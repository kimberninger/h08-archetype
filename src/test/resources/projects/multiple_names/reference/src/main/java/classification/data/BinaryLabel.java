package classification.data;

/**
 * Dieses Enum beschreibt allgemeine Labels für die binäre Klassifikation.
 * 
 * @author Kim Berninger
 * @version 1.1.0
 */
public enum BinaryLabel implements Label {
    /** Das Label für Positivbeispiele. */
    POSITIVE,

    /** Das Label für Negativbeispiele. */
    NEGATIVE;
}
